// ########################################### //
// ### RVAS-CORE MASTER CONFIGURATION FILE ### //
// ########################################### //

// Internal use only. Do not change or you'll break the entire thing.
config.version = 22

// ------------------ //
// --- IMPORTANT  --- //
// ------------------ //

// uuid for the full-access in-game owner's account
adminid = a6b6e3a1-a1ec-4fee-9d6d-f5e495c3e9d7

// ign for the full-access in-game owner's account
admin = sinse420

// Verbose console output (may globally broadcast some events info in chat)
debug = false

// Remove less technical but otherwise useful console output (may globally broadcast some events info in chat)
devesp = false

// ----------------------- //
// --- MOVEMENT CHECKS --- //
// ----------------------- //

// Instantly kill players above y=127 in the nether
movement.kill.roof = false

// Instantly kill players under the bedrock floor
movement.kill.floor = false

// Use players' movement events to delete existing illegal items from the world
movement.illegals.check = false

// ------------------------- //
// --- PLACEMENT PATCHES --- //
// ------------------------- //

// Prevent creative mode players from duplicating items
protect.lock.creative = true

// Prevent anyone from placing blocks on the nether roof
protect.roof.noplacement = false

// Whether to cancel placement of all blocks that are unobtainable in no-commands survival-mode
// (true = cancel placements)

protect.banned.place = true
protect.banned.place.ops = true

// --------------------------- //
// --- BLOCK BREAK PATCHES --- //
// --------------------------- //

// Protect bedrock floor (in all dimensions) / roof (in the nether)
//** DOES NOT PREVENT VANILLA BLOCK BREAK EXPLOITS**//
// You can enable and disable those exploits in the paper.yml config-file on your server ("allow-permanent-block-break-exploits")

protect.bedrock.roof = true
protect.bedrock.floor = true

// Force non-ops into survival mode on block break events
protect.gamemode.onbreak = true

// ---------------------------- //
// --- ILLEGAL ITEM PATCHES --- //
// ---------------------------- //

// Skip illegal checks for ops?
skip.ops = true

// Check for illegal items? (false will disable most illegal item checks)
item.illegal = true

// Can items be over-stacked?
item.illegal.stacked = true

// Are player heads illegal?
item.illegal.heads = true

// Enable or disable unbreakable items. false = disabled [default] || true = enabled
item.illegal.unbreakable = false

// Enable or disable exclusive enchants. false = disabled [default] || true = enabled
item.illegal.exclusive = false

// Enable or disable invalid enchants. false = disabled [default] || true = enabled
item.illegal.invalid = false

// Remove these items from player inventories when they join
item.illegal.onjoin = true

// Enable or disable aggressive checks. false = disabled || true = enabled [default]
// Can cause some lag on lower-end servers
item.illegal.agro = false

// Number of players to cache chunks for in agro check mode
// Set this to a number a little larger than the typical player pop
item.illegal.agro.player_count = 10

// Number of chunks per player to cache
item.illegal.agro.chunk_count = 1024

// How frequently to flush the cache
// Set to -1 to disable
item.illegal.agro.flush_period = 3600

// -------------- //
// --- VOTING --- //
// -------------- //

// Enable or disable vote reward. false = disabled || true = enabled [default]
vote.enable = true

// Enable or disable manual execution of vote reward. Use for debug only. false = disabled [default] || true = enabled
vote.debug = true

// Vote reward multiplier. default = 1
vote.multiplier = 1

// Vote reward also heals player to max health. false = disabled || true = enabled [default]
vote.heal = false

// -------------- //
// --- MUTING --- //
// -------------- //

// Enable vote muting. false = disabled || true = enabled [default]
mute.enabled = true

// Percent of population that needs to vote before a mute is ordered. Default: 40
mute.pop = 51

// Enable voting by already-muted persons. false = disabled || true = enabled [default]
mute.hypocrisy = true

// ----------- //
// --- LAG --- //
// ----------- //

// Global wither limit. default = 710
wither.limit = 710

// Global wither skull age limit. default = 2400 (2 minutes in ticks if server is running at 20TPS)
wither.skull.max_age = 2400

// Global speed limit when TPS is between 16 and 20. default = 76
speedlimit.tier_one = 76

// Global speed limit when TPS is between 14 and 16. default = 48
speedlimit.tier_two = 48

// Global speed limit when TPS is between 10 and 14. default = 32
speedlimit.tier_three = 32

// Global speed limit when TPS is between 7 and 10. default = 26
speedlimit.tier_four = 26

// Global speed limit when TPS is 7 or less. default = 20
speedlimit.tier_five = 20

// Medium kick speed limit (grace period is 2 seconds)
speedlimit.medium_kick = 80

// Hard kick speed limit (insta-kick if exceeded)
speedlimit.hard_kick = 100

// Reconnect delay when kicked by exceeding the speed limit, in ms. default = 10000 (10s)
speedlimit.rc_delay = 10000

// Reconnect delay when reconnecting normally, in ms. default = 5000 (5s). Set to 0 to disable.
speedlimit.rc_delay_safe = 5000

// Disables speed limit warnings and only kicks 
speedlimit.agro = false

// The tps under which the auto-restart timer ticks
restart.threshold = 7.10

// ------------ //
// --- SPAM --- //
// ------------ //

// Enable the anti-spam system
spam.enable = true

// Minimum similarity to prevent a message being re-sent. default = 95 (percent)
spam.min_similarity = 95

// Time required to wait between messages, in ms. default = 1000 (ms)
spam.wait_time = 1000

// Required number of violations to punish. default = 20 (vl)
spam.minimum_vl = 20

// Reconnect delay after being kicked for spam. default = 5000 (ms)
spam.rc_delay = 5000

// Allow ops to bypass anti-spam. default = true
spam.ops = true

// ------------------ //
// --- AESTHETICS --- //
// ------------------ //

// Force the server IP. true/false. default = false
motd.force = false

// Server IP to force. string value. default = RVAS
motd.force.name = RVAS

// Discord link. string value. default = tbd
discord.link = tbd

// ------------ //
// --- MISC --- //
// ------------ //

// Enable Discord analytics notifications. false = disabled [default] true = enabled
analytics.enabled = false

// Discord Bot ID for analytics notifications. Default: empty. 
analytics.bot_id = 

// Force survival mode on relog except OPs false = disabled true = enabled [default]
misc.survival = true

// Enable or disable the prank /kit command. false = disabled true = enabled [default]
funny.kit = false
